# Copy this file to secrets.toml (local) or paste into the
# Streamlit Cloud → App → Settings → Secrets box (deployed).
# secrets.toml itself is gitignored and must never be committed.

[tke]
username = "your-portal-username@example.com"
password = "your-portal-password"
